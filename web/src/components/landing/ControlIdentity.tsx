import React from 'react';
import { RandomGraphicElement } from '../'

export default ({ }) => (
    <RandomGraphicElement elements={5}>
        <div className="control-identity-section">
            <h3>You control your own identity.</h3>
        </div>
    </RandomGraphicElement>
);