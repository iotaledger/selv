import React from 'react';
import { RandomGraphicElement } from '../'

export default ({ }) => (
    <RandomGraphicElement elements={10}>
        <div className="users-business-section">
            <h1>For users</h1>
        </div>
    </RandomGraphicElement>
);